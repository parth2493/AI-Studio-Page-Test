# AI-Studio-Page-Test
AI Studio for 3d generation page test


## Local development

Run this page through a local HTTP server instead of opening `index.html` with `file://`, so the video, particle animation, and scroll effects initialize consistently.

```bash
python3 -m http.server 4173
```

Then open `http://localhost:4173/`.
